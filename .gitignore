# Node (backend)
node_modules/
dist/
*.log

# Env / secrets — never commit
.env
**/.env

# OS / editor
.DS_Store
.vscode/
coverage/

# Flutter app ignores are handled by app/.gitignore (generated by `flutter create`).
# pubspec.lock IS committed for reproducible CI builds.
